# GiinScore 改善計画 - 多くの人に使ってもらえるプロダクトへ

## 現状の課題サマリー

GiinScoreは4軸スコアリング・パーセンタイル正規化・重みカスタマイズなど基盤は整っているが、
**「一般ユーザーが繰り返し使いたくなる魅力」** がまだ不足している。

主な課題:
1. **静的なスナップショット** - 最新スコアだけで変化が見えない
2. **個人単位の閲覧のみ** - 比較・集計・俯瞰の機能がない
3. **データの鮮度** - 手動パイプライン実行のみ
4. **共有・拡散の仕組みがない** - SNS連携なし
5. **参議院投票/衆議院法案のスクレイパーが壊れている** - データに欠損

---

## Phase 1: データ信頼性の確立（最優先）

> スコアの信頼性がなければ、どんなUIも無意味

### 1-1. 参議院投票スクレイパーの修正
- Session 213で114ページ取得済みだが0件登録
- 起立採決ページのHTMLパース修正
- billとの紐付けロジック（title部分一致）の改善

### 1-2. 衆議院法案詳細スクレイパーの修正
- Session 213で0件。ページ構造変更への対応
- billsテーブルが空の場合のフォールバック

### 1-3. データ品質ダッシュボード
- 管理用エンドポイント: `GET /api/v1/admin/data-quality`
- 会期ごとのデータ充足率（議員数、発言数、投票率、法案数）
- 欠損データのアラート

### 1-4. 定期自動パイプライン実行
- GitHub Actions scheduled workflow（週次 or 国会開会中は日次）
- パイプライン間の依存関係管理
- 失敗時のSlack/Discord通知

---

## Phase 2: 分析価値の向上（ユーザーの「知りたい」に応える）

> 「この議員はどう変わった？」「この政党はどうなの？」に答える

### 2-1. スコア時系列グラフ
- 議員詳細ページに会期ごとのスコア推移折れ線グラフ追加
- 既存データ（MemberScore.session_id）を活用、追加開発は最小限
- 4軸個別 + 総合スコアの推移表示

### 2-2. 議員比較機能
- `/compare?ids=1,2,3` ページ新設
- 最大4名の議員をレーダーチャート重ね合わせ
- テーブルで各軸スコアの横並び比較
- ランキングページから「比較に追加」ボタン

### 2-3. 党派別・役職別統計API & UI
- `GET /api/v1/scores/by-party` - 政党ごとの平均・中央値・分布
- `GET /api/v1/scores/by-role` - 役職カテゴリ別統計
- トップページに「政党別ランキング」セクション追加
- 棒グラフ or ヒートマップで政党間比較

### 2-4. 投票パターン分析
- 党議拘束からの逸脱度を可視化
- 「造反率」指標の追加（所属政党の多数派と反対票を投じた割合）
- VBS breakdownに党の投票方針との乖離率を追加

---

## Phase 3: ユーザー体験の向上（使い続けたくなるUI）

> 「面白い」「便利」「人に教えたい」を実現

### 3-1. ソーシャルシェア機能
- 議員詳細・ランキングページにシェアボタン（X/LINE/Facebook）
- OGP画像の動的生成（議員名+グレード+レーダーチャート画像）
- `/api/og?member_id=123` でOGP画像を動的生成

### 3-2. 選挙区ベースの議員検索
- 「あなたの選挙区の議員」機能
- 都道府県/ブロック選択 → 該当議員表示
- 地図UI（都道府県クリッカブルマップ）

### 3-3. CSV/PDFエクスポート
- ランキングページ: 「CSVダウンロード」ボタン
- 議員詳細ページ: 「スコアレポートPDF」
- `GET /api/v1/export/ranking?format=csv`

### 3-4. お気に入り/ウォッチリスト
- ローカルストレージに議員IDを保存
- ヘッダーに「ウォッチリスト」リンク
- ウォッチ中の議員のスコア変動通知（将来）

### 3-5. モバイルUX最適化
- ランキングテーブルのカード表示切替（モバイル時）
- スワイプでの議員比較
- タッチフレンドリーなフィルタUI

### 3-6. ダークモード
- Tailwind CSS v4のダークモードサポート活用
- ヘッダーにトグルスイッチ

---

## Phase 4: 拡散・成長のための基盤

> 一般ユーザー・メディア・研究者に使ってもらう

### 4-1. ランディングページの強化
- スコアの意味を30秒で理解できるビジュアル説明
- 「今週のハイライト」（スコア急上昇/下降議員）
- 具体的なユースケース提示（有権者/メディア/研究者向け）

### 4-2. SEO強化
- 議員名での検索流入を狙う（各議員ページのmeta最適化）
- 構造化データ（JSON-LD）の追加
- `sitemap.xml` の動的生成（全議員・全法案ページ）

### 4-3. 公開API & ドキュメント
- OpenAPIスペックの公開（Swagger UIは既存）
- API利用ガイド・サンプルコード
- レート制限の設定（一般公開向け）

### 4-4. パフォーマンス最適化
- Redis キャッシュ（ランキングTOP100、統計情報）
- ISR (Incremental Static Regeneration) の活用
- スコア更新時のキャッシュ無効化

### 4-5. 多言語対応（英語）
- next-intl導入
- 主要ページの英語版
- 「Japanese Parliament Scorecard」としての国際発信

---

## 実装優先度マトリックス

| 優先度 | 項目 | 工数 | ユーザー価値 | 理由 |
|--------|------|------|-------------|------|
| **P0** | 1-1 参議院投票スクレイパー修正 | S | 高 | データ欠損は致命的 |
| **P0** | 1-2 衆議院法案スクレイパー修正 | S | 高 | 同上 |
| **P1** | 2-1 スコア時系列グラフ | S | 高 | 既存データ活用、開発コスト低 |
| **P1** | 2-2 議員比較機能 | M | 高 | 最も「面白い」機能 |
| **P1** | 3-1 ソーシャルシェア | S | 高 | 拡散のための必須機能 |
| **P2** | 2-3 党派別統計 | M | 高 | 政治に興味ある層への訴求 |
| **P2** | 3-2 選挙区検索 | M | 高 | 「自分ごと化」の鍵 |
| **P2** | 1-4 自動パイプライン | M | 中 | 運用負荷軽減 |
| **P2** | 3-3 CSV/PDFエクスポート | S | 中 | 研究者・メディア向け |
| **P3** | 2-4 投票パターン分析 | L | 高 | 分析価値大だが工数も大 |
| **P3** | 1-3 データ品質ダッシュボード | M | 中 | 運用支援 |
| **P3** | 3-4 お気に入り機能 | S | 中 | リピート率向上 |
| **P3** | 3-5 モバイルUX最適化 | M | 中 | モバイルユーザー増加 |
| **P3** | 3-6 ダークモード | S | 低 | ユーザー要望次第 |
| **P4** | 4-1 LP強化 | M | 高 | 初見ユーザーの離脱防止 |
| **P4** | 4-2 SEO強化 | S | 高 | 流入増 |
| **P4** | 4-3 公開API | M | 中 | 開発者コミュニティ形成 |
| **P4** | 4-4 パフォーマンス最適化 | M | 中 | スケール対応 |
| **P4** | 4-5 多言語対応 | L | 低 | 国際展開（長期） |

工数: S=1-2日, M=3-5日, L=1-2週間

---

## 次のアクション

1. **Phase 1 (P0)** から着手 - スクレイパー修正でデータ完全性を確保
2. **Phase 2 の P1 項目** - 時系列グラフ + 比較機能で「面白さ」を追加
3. **シェア機能** - 拡散の仕組みを早期に入れる
